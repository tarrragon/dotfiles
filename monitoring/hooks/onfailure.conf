# 通用失敗告警 drop-in。deploy.sh 依 units.txt 裝到各 <unit>.service.d/onfailure.conf。
# %n 展開成該 unit 全名 → 觸發 alert@<unit>.service（notify-failure）。
# drop-in 是附加的、不動原 unit 檔；OnFailure 是 list、會累加不會覆蓋 unit 原有的。
[Unit]
OnFailure=alert@%n.service
