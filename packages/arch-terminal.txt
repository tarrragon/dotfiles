# Arch terminal 層 — 終端機 / 遠端工作的 QoL 工具鏈（headless server 也適用）
# 字型不在這層：字型只在有畫面的機器有意義、歸 desktop 層
# oh-my-zsh / powerlevel10k / zsh plugins 由 install.sh 共通層 git clone（非 pacman 套件）

# shell 支援
zoxide            # 目錄跳轉（z / zi；init 在 zsh/tools.zsh；autojump 已移到 AUR、zoxide 官方 repo 有）
jq                # JSON 處理（ntfy 桌面訂閱解析 /json 串流、通用 JSON CLI）

# stowed config 對應的 binary
broot             # 互動式目錄樹瀏覽器
btop              # 系統資源監控 TUI
zellij            # 終端機多工器（SSH 無人值守任務靠它）

# 核心 CLI
github-cli        # gh（PR / issue / auth 操作）
fzf               # 模糊搜尋

# 常用 CLI
ripgrep           # rg（grep 替代）
fd                # find 替代
bat               # cat 替代、帶語法高亮
lazygit           # Git 操作 TUI
git-delta         # diff 語法高亮 pager（.gitconfig core.pager 指向它）
yazi              # 終端機檔案管理器
tig               # Git 歷史 TUI
ranger            # 終端機檔案管理器（三欄式）

# Claude Code 的 runtime（CC 本體由 install.sh 共通層用原生 installer 裝）
nodejs
npm
