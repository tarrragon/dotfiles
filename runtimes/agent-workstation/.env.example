# 複製成 .env（同目錄）並填入真值。.env 已被 repo .gitignore 排除、絕不進 git。

# Claude Code → Anthropic 的認證（agent 能不能運作）。
# 由 `claude setup-token` 產生（見 README「一次性認證」）、有效約一年。
CLAUDE_CODE_OAUTH_TOKEN=

# GitHub → git 操作的認證（clone/push 私有 repo、gh 開 PR）。跟上面那顆是正交的兩顆機密、同一個注入模式。
# 用 fine-grained PAT、只授需要的 repo + 最小權限（Contents / Pull requests）、短輪替——曝露面同 OAuth token。
# 產生：GitHub Settings → Developer settings → Personal access tokens → Fine-grained。留空則只能匿名讀 public repo。
GH_TOKEN=

# ntfy 推播的 topic（猜到就能發/收你的通知、屬機密）。Stop hook 的 settings.json 用 $NTFY_TOPIC 引用它、
# 設定檔本身不含機密、真值在此注入。留空則沒設 topic 的 hook 會發到空路徑、等於不通知。
# 注意：這只注入「topic」；hook 裡的 `Title:` 是會被公共 server 看到的顯示字、非機密、直接寫在 settings.json。
NTFY_TOPIC=
